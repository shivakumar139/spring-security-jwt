spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ecom?autoReconnect=true&useSSL=false&createDatabaseIfNotExist=true
    username: root
    password: root1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format-sql: true
  main:
    allow-circular-references: true